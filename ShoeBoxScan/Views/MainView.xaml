<Window x:Class="ShoeBoxScan.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoeBoxScan.Views"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="Base">
                <MenuItem Header="Import Excel File" Command="{Binding ImportCommand}"></MenuItem>
                <MenuItem Header="Scan Shoe Boxes" Command="{Binding ScanCommand}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Inquiry">
                <MenuItem Header="Query Orders" Command="{Binding QueryCommand}"></MenuItem>
            </MenuItem>
        </Menu>
    </Grid>
</Window>
